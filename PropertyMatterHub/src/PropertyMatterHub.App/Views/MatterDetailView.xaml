<UserControl x:Class="PropertyMatterHub.App.Views.MatterDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Grid>
    <ui:ProgressRing IsIndeterminate="True" HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibility}}">
      <StackPanel Margin="28,20,28,28">

        <!-- Header -->
        <StackPanel Margin="0,0,0,20">
          <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
            <ui:Badge Content="{Binding SelectedMatter.MatterRef}" Margin="0,0,10,0" />
            <ui:Badge Content="{Binding SelectedMatter.Status}" />
          </StackPanel>
          <TextBlock Text="{Binding SelectedMatter.Title}" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
          <TextBlock Margin="0,4,0,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}">
            <Run Text="{Binding SelectedMatter.Client.Name}" />
            <Run Text=" · " />
            <Run Text="{Binding SelectedMatter.PracticeArea}" />
          </TextBlock>
        </StackPanel>

        <!-- Tab strip -->
        <ui:TabView Margin="0,0,0,16">
          <ui:TabViewItem Header="Overview">
            <StackPanel Margin="0,16,0,0">
              <!-- Client details -->
              <ui:CardControl Header="Client" Icon="{ui:SymbolIcon Symbol=Person24}" Margin="0,0,0,12">
                <StackPanel Margin="0,8,0,8">
                  <TextBlock Text="{Binding SelectedMatter.Client.Name}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding SelectedMatter.Client.Phone}" FontSize="13"
                             Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                  <TextBlock Text="{Binding SelectedMatter.Client.Email}" FontSize="13"
                             Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                  <TextBlock Text="{Binding SelectedMatter.Client.Address}" FontSize="13"
                             Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
              </ui:CardControl>

              <!-- Key dates -->
              <TextBlock Text="Key Dates" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" />
              <ItemsControl ItemsSource="{Binding KeyDates}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Margin="0,4" Padding="12,10" CornerRadius="8"
                            Background="{ui:ThemeResource ControlFillColorDefaultBrush}">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding Date, StringFormat='dd/MM/yyyy'}"
                                   Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>

              <!-- Notes -->
              <TextBlock Text="Notes" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,8" />
              <ui:TextBox Text="{Binding SelectedMatter.Notes, UpdateSourceTrigger=PropertyChanged}"
                          PlaceholderText="Add notes…" AcceptsReturn="True" Height="100" />

              <ui:Button Content="Save Changes" Command="{Binding SaveCommand}"
                         Icon="{ui:SymbolIcon Symbol=Save24}" Margin="0,12,0,0" />
            </StackPanel>
          </ui:TabViewItem>

          <ui:TabViewItem Header="Emails">
            <ItemsControl ItemsSource="{Binding Emails}" Margin="0,12,0,0">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border Margin="0,4" Padding="12,10" CornerRadius="8"
                          Background="{ui:ThemeResource ControlFillColorDefaultBrush}">
                    <StackPanel>
                      <TextBlock Text="{Binding Subject}" FontWeight="SemiBold" />
                      <TextBlock Margin="0,2,0,0" FontSize="12"
                                 Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}">
                        <Run Text="{Binding From}" />
                        <Run Text=" · " />
                        <Run Text="{Binding SentAt, StringFormat='dd/MM/yyyy HH:mm'}" />
                      </TextBlock>
                      <TextBlock Text="{Binding Snippet}" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0"
                                 Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ui:TabViewItem>

          <ui:TabViewItem Header="Calendar">
            <ItemsControl ItemsSource="{Binding CalendarEvents}" Margin="0,12,0,0">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border Margin="0,4" Padding="12,10" CornerRadius="8"
                          Background="{ui:ThemeResource ControlFillColorDefaultBrush}">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="{Binding StartUtc, StringFormat='dd/MM HH:mm'}" FontSize="12"
                                 Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                      <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ui:TabViewItem>
        </ui:TabView>

      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
